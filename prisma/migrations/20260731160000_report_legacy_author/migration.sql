-- AlterTable
-- 게시판이 공용 저장소에 있던 시절의 글에는 계정이 없다. 그때는 로그인 없이 아무
-- 이름으로 쓸 수 있었기 때문이다. 그 글들을 되살리려면 작성자 자리가 비어 있을 수
-- 있어야 한다. 없는 계정을 지어내 붙이면 그 사람이 쓴 것처럼 보이므로, 없는 것은
-- 없는 대로 둔다. 이 칸이 비었다는 것은 "옛날 글" 이라는 뜻이고 본인 삭제는 안 된다.
--
-- 새로 쓰는 글은 여전히 로그인해야 하며 API 가 그것을 막는다. 이 칸을 옵셔널로
-- 여는 것이 "누구나 쓸 수 있다" 로 되돌아간다는 뜻은 아니다.
ALTER TABLE "Report" ALTER COLUMN "userId" DROP NOT NULL;
