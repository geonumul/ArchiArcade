-- AlterTable
-- 글을 올린 곳의 나라. 어느 나라 사람이 쓴 글인지 보여 주려고 받는다.
-- 언어만으로는 어긋난다 - 한국 사람이 영어로 쓸 수도 있고 그 반대도 있다.
--
-- IP 로 알아내되 IP 자체는 저장하지 않는다. 나라 하나로는 누구인지 좁혀지지 않으므로
-- 도시나 좌표까지 가지 않는다. 개인정보 보호법 제15조제1항제6호(정당한 이익)에 기대고,
-- 처리방침에 무엇을 왜 받는지 적는다.
--
-- 기존 글은 비어 있다. 그때는 화면이 언어로 대신 표시한다.
ALTER TABLE "Report" ADD COLUMN "country" TEXT;
